import { shuffleArray } from './utils'

export const fetchQuestions = async () => {
    // const fs = require('fs');
    // // TODO: connect to json info (path in string)
    // const jsonData = require('');

    // fs.readFile(jsonData, 'utf8', function (err, data) {
    //     try {
    //         data = JSON.parse(data)
    //         console.log(data)
    //         // TODO: Edit this to reflect data, but shuffling the answer within the wrong options means it's not always in the same place
    //         return data.results.map((question) => (
    //             {
    //                 ...question,
    //                 answers: shuffleArray([question.correctAnswer, ...question.incorrectAnswers])
    //             }
    //         ))
    //     } catch (e) {
    //         // Catch error in case file doesn't exist or isn't valid JSON
    //     }
    // });
}